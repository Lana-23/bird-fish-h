{
  "name": "bird-fish-tracker-android",
  "version": "1.0.0",
  "description": "Bird & Fish Tracker - Android App",
  "main": "app.js",
  "scripts": {
    "build:web": "mkdir -p www && cp index.html styles.css app.js birds-*.js fish-*.js assets www/",
    "sync": "npm run build:web && npx cap sync android",
    "open:android": "npx cap open android",
    "cap:copy": "npm run build:web && npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
