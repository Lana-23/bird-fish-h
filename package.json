{
  "name": "bird-fish-tracker-android",
  "version": "1.0.0",
  "description": "Bird & Fish Tracker - Android App",
  "main": "app.js",
  "scripts": {
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build": "echo 'Web app is already built - just copying to android/www'",
    "cap:copy": "npx cap copy android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
